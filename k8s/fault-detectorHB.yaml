apiVersion: apps/v1
kind: Deployment
metadata:
  name: fault-detectorHB
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: fault-detectorHB
  template:
    metadata:
      labels:
        app: fault-detectorHB
    spec:
      containers:
      - name: fault-detectorHB
        image: fault-detectorHB:v1
        ports:
        - containerPort: 1234
        envFrom:
        - configMapRef:
            name: fault-detector-env
        livenessProbe:
          httpGet:
            path: /ping
            port: 1234
          initialDelaySeconds: 40
          timeoutSeconds: 3
        readinessProbe:
          httpGet:
            path: /ping
            port: 1234
          initialDelaySeconds: 15
          timeoutSeconds: 3

